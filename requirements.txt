# Core dependencies for Phase 1 features
tiktoken>=0.5.0          # Token counting for various LLM models
pyperclip>=1.8.0         # Clipboard integration
pathspec>=0.11.0         # .gitignore/.gptignore pattern matching

# Python standard library modules used (no external dependencies required)
# - tkinter (included with Python on Windows)
# - json
# - datetime
# - pathlib
# - os
# - re (for PII sanitization)

# Optional: For creating standalone executable
pyinstaller>=5.0
